# gardusig/static-puzzles-frontend

Chrome-first puzzle picker — React + Vite launcher from static-puzzles.

## Quick start

```bash
cd src/launcher/frontend
npm install
npm run dev
```

http://localhost:8091

## Layout

- `src/launcher/frontend/` — Vite app (`package.json`, `src/`, Tailwind)
